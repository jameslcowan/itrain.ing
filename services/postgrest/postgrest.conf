# PostgREST — REST API over PostgreSQL (no ORM)
# Install: infra/server/install-postgrest.sh
# Secrets: /etc/itrain/postgrest.env (not in git)

db-uri = "postgres://authenticator:@127.0.0.1:5432/itrain"
db-schemas = "public"
db-anon-role = "web_anon"
server-host = "127.0.0.1"
server-port = 3000

# v12+ recommended
openapi-mode = "ignore-transaction"
